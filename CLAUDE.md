# Info Platform 协作记忆

更新日期：2026-04-12

## 用途

- 本文件记录仓库当前真实可运行的产品状态。
- Claude、Codex 等协作代理都应以这里的规则为准。
- 如果这里和旧代码注释、旧截图、旧文档冲突，以这里为准。

## 当前导航

- `抓取任务`
- `词条设置`
- `系统设置`
- 管理员额外有 `admin`

## 当前页面规则

### 抓取任务页

- 顶部右侧保留 `智能推荐` 和 `立即抓取`。
- `智能推荐` 生成的结果必须是短关键词，不是句子、标题或问题句；中文词条优先控制在 `2-5` 个字。
- 左侧模块是 `关键词列表`，右侧模块是 `关键词`。
- `关键词` 模块只负责新增词条。
- `关键词列表` 展示当前词条与分组。
- 默认分组是 `全部`。
- 在 `全部` 视图下可以把当前词条整体生成一个新分组。
- 删除分组时只移除 `group_name`，不删除词条本身。
- 历史抓取任务列表需要实时展示状态与耗时。
- 任务耗时口径固定为：`created_at -> completed_at`，也就是从任务下发到摘要生成完成并可查看。

### 词条设置页

- 页面按“词条”维度查看历史，不按批次 digest 维度组织。
- 页面标题右侧提供两个按钮：
  - `清空当前词条`
  - `清空所有词条`
- 顶部展示所有抓取过的词条，支持搜索。
- 点击词条后，下方按天展示该词条历史。
- 历史卡片中的来源链接必须跟在对应 bullet / 小标题末尾，不集中放在卡片顶部。
- 来源可见文本要尽量收口成短来源名，不要展示大段裸 URL。
- 清空当前词条历史后，该词条当天可重新抓取。
- 一键清空全部词条历史后，当前用户全部词条当天都可重新抓取。
- 删除词条配置时，不应删除历史抓取记录；历史词条仍应保留，用户之后可重新加入。

### 系统设置页

- 当前保留模块：
  - 账户
  - 界面语言
  - LLM 配置
  - Webhook 推送
  - API 用量
- 当前已下线模块：
  - Email 通知
  - 定时调度
  - RSS / feed token
  - 分组路由
  - Web Push
  - 旧来源管理

## 当前抓取与摘要规则

- 抓取以“词条”为单位执行。
- 同一词条默认一天最多抓取一次。
- 如果当天已存在该词条的 `crawl_results`，再次抓取会复用当天内容。
- 如果在词条设置中清空该词条历史，则当天可重新抓取，不再复用旧结果。
- 如果一键清空全部词条历史，则全部词条当天都可重新抓取。
- 只要任务进入摘要生成阶段，前端应继续轮询，直到摘要可查看后立刻刷新为完成态。
- 任务耗时的统计口径固定为：`created_at -> completed_at`。
- Google News RSS 抓取应尽量在“够用就停”，避免每个关键词跟进过多文章导致单次任务过慢。
- 中文界面下，摘要应尽量输出简体中文；原始抓取数据、原始来源 URL 不做改写。

## 当前代码与数据约定

- 已下线功能的旧代码入口已经移除。
- 已下线功能对应旧表已经迁掉：
  - `notification_routes`
  - `push_subscriptions`
  - `sources`
  - `user_email_configs`
  - `user_schedule_configs`
- `crawl_results.source_id` 已删除。
- 现有数据库迁移以 Alembic 为准。

## 本地开发方式

- 当前默认开发方式是 Mac 原生运行，不依赖 Docker。
- 基础设施由 `brew services` 管理：`postgresql@16`、`redis`
- 应用服务由 `start.sh` 管理：`playwright`、`backend`、`worker`、`frontend`

```bash
./start.sh setup
./start.sh
./start.sh stop
./start.sh status
```

- 日志输出到 `logs/` 目录。
- Python 虚拟环境位于 `backend/venv/`。
- Playwright 微服务代码在 `docker/playwright/main.py`，但本地仍由原生 Python 进程启动。

## 文档约定

- `README.md` 用于对外和对使用者的稳定说明。
- `CLAUDE.md` 用于仓库内部协作记忆，强调“当前真实状态”。
- 如果二者冲突，开发与排障时优先看 `CLAUDE.md`。
