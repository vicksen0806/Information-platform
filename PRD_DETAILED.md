# Info Platform 详细 PRD

更新日期：2026-04-12

## 1. 文档目的

这份文档不是对外展示版，而是面向项目维护者、接手开发者和产品负责人，用来理解当前项目的：

- 产品边界
- 用户主流程
- 业务规则
- 页面结构
- 后端模块划分
- 数据模型关系
- 异步任务链路
- 当前已知技术取舍

本文档只描述“当前仍在运行”的系统，不包含已经下线的功能模块。

## 2. 产品定义

Info Platform 是一个面向多用户的“关键词持续追踪 + 内容抓取 + 摘要生成”平台。

它的核心价值不是做通用搜索，而是帮助用户围绕固定主题持续积累信息，并把原始网页内容压缩为可阅读、可分享、可导出的摘要卡片。

产品当前的核心特征有三点：

1. 输入单位是“关键词”
2. 执行单位是“抓取任务”
3. 查看单位既支持“任务”，也支持“词条历史”

## 3. 产品目标

### 3.1 业务目标

- 降低用户持续追踪外部信息的成本
- 把零散公开内容整理成结构化摘要
- 保留主题历史，支持用户回看和复抓
- 让任务状态、摘要生成进度和耗时对用户透明

### 3.2 体验目标

- 用户能快速完成“新增词条 -> 立即抓取 -> 查看摘要”
- 用户能理解为什么某次任务快或慢
- 用户能在同一天内通过清空历史重新抓取特定词条
- 用户看到的是按关键词拆分的摘要，而不是混杂在一起的大段文本

## 4. 目标用户

### 4.1 核心用户

- 持续跟踪行业、地域、公司、技术话题的个人知识工作者
- 需要做轻量信息监测的小团队
- 需要把外部信息转化为简报内容的运营、研究、咨询、投研用户

### 4.2 使用场景

- 跟踪地区或国际关系议题，如“中国”“美国”“中东”
- 跟踪科技或 AI 领域话题，如“AI”“大模型”“Gemini”“Claude”
- 跟踪公司、品牌、竞品、事件或政策类主题

## 5. 范围与非范围

## 5.1 当前功能范围

- 账户登录与基础权限
- 关键词管理
- 关键词分组
- 智能推荐关键词
- 手动触发抓取
- 实时查看抓取任务状态和耗时
- LLM 摘要生成
- 摘要详情查看、分享、导出、收藏、反馈
- 按词条查看历史
- 清空当前词条历史
- 一键清空全部词条历史
- Webhook 平台推送
- 管理员后台基础能力

## 5.2 当前不在范围内

- 邮件通知
- 定时调度抓取
- RSS / Feed Token 外部订阅
- Web Push
- 旧来源管理后台
- 企业级复杂权限与组织体系

## 6. 核心概念

### 6.1 关键词（Keyword）

用户持续关注的主题词。系统当前一切抓取和摘要都围绕关键词组织。

字段层面，关键词包含：

- `text`：词条文本
- `url`：可选，若用户指定抓取 URL，则优先使用该 URL
- `source_type`：抓取类型，常见为 `search` / `rss` / `webpage`
- `group_name`：所属分组
- `requires_js`：是否需要 Playwright 渲染
- `last_crawled_at`：最近抓取时间

### 6.2 抓取任务（Crawl Job）

一次“立即抓取”会生成一个抓取任务。抓取任务本质上是某个用户在某个时间点，对当时启用关键词集合的一次执行快照。

它负责记录：

- 任务创建时间
- 任务开始时间
- 任务完成时间
- 当前状态
- 是否找到新内容
- 是否预期生成摘要
- 摘要错误信息

### 6.3 抓取结果（Crawl Result）

每个抓取任务会为每个关键词生成一条抓取结果。它是“任务”和“词条”之间的中间层。

记录内容包括：

- 词条文本
- 原始抓取内容
- 内容哈希
- HTTP 状态码
- 抓取时间
- 错误信息

### 6.4 摘要（Digest）

摘要是对一次抓取任务中“有有效内容的词条集合”的 LLM 输出结果。

摘要并不只是一个总文本，而是包含：

- 摘要标题
- Markdown 正文
- 本次摘要实际使用的关键词列表
- 来源数量
- token 用量
- 使用的 LLM 模型
- 关联的抓取任务 ID

### 6.5 词条历史

词条历史不是直接按 digest 批次组织，而是把同一个关键词在不同日期下的摘要内容重新聚合出来，形成按词条维度浏览的历史视图。

## 7. 用户主流程

## 7.1 新建并抓取

1. 用户进入“抓取任务”页
2. 用户在右侧输入框新增关键词
3. 用户可手动调整当前关键词集合
4. 用户点击“立即抓取”
5. 前端创建一条新的抓取任务并插入任务列表顶部
6. 后端异步执行抓取
7. 抓取完成后，如果有有效内容则继续生成摘要
8. 摘要生成完成后，任务状态切换为已完成，用户可以点击“查看摘要”

## 7.2 智能推荐

1. 用户点击“智能推荐”
2. 后端结合用户近 15 天实际抓取/使用过的关键词，请求 LLM 返回推荐词
3. 推荐结果在前端展开显示
4. 用户可以逐个添加到当前关键词集合

当前推荐规则要求：

- 必须是短关键词
- 不是句子
- 不是新闻标题
- 中文关键词优先控制在 2-5 个字

## 7.3 查看任务结果

任务列表按时间倒序展示。

每条任务会显示：

- 状态标签
- 时间戳
- 已用时
- 查看摘要入口或重试入口

耗时规则固定为：

- 起点：`created_at`
- 终点：`completed_at`
- 意义：从任务下发到摘要生成完成且用户可查看

## 7.4 按词条查看历史

1. 用户进入“词条设置”页
2. 页面展示历史抓取过的词条列表
3. 用户选择某个词条
4. 下方按日期展示该词条历史摘要

这个页面不是按“某次任务”的视角，而是按“同一词条持续发生了什么”的视角组织内容。

## 7.5 清空历史并重新抓取

用户有两种清空方式：

- `清空当前词条`
- `清空所有词条`

清空后的结果：

- 对应词条的历史 `crawl_results` 被删除
- 相关词条的 `last_crawled_at` 被回退
- 如果某些抓取任务因此变成空壳，会同步删除
- 这样同一个词条在同一天就可以再次抓取，不会命中“当天复用”

注意：

- 删除词条配置时，不删除历史记录
- 历史记录和当前词条配置是两个概念

## 8. 页面与信息架构

## 8.1 顶层导航

当前导航为：

- `抓取任务`
- `词条设置`
- `系统设置`
- 管理员额外有 `admin`

## 8.2 抓取任务页

页面可分为三块：

### A. 顶部操作区

- `智能推荐`
- `立即抓取`

### B. 当前关键词区

左侧是 `关键词列表`：

- 显示当前启用的关键词
- 支持按分组查看
- 支持删除关键词
- 支持整体生成分组

右侧是 `关键词`：

- 只负责新增关键词

### C. 抓取任务区

- 展示每次抓取的进度与状态
- 任务进行中前端每秒轮询
- 摘要完成后立即刷新为完成态

## 8.3 词条设置页

页面由三部分构成：

### A. 顶部说明区

- 页面标题和副标题
- 两个操作按钮：
  - `清空当前词条`
  - `清空所有词条`

### B. 词条列表区

- 展示所有有历史的词条
- 支持搜索
- 显示该词条累计出现天数和最近抓取时间

### C. 历史详情区

- 展示所选词条的历史摘要
- 以日期为主键组织
- 每个卡片内部渲染 Markdown
- 来源链接必须跟在对应 bullet 的末尾
- 链接文案需收口为短来源名，不显示大段 URL

## 8.4 系统设置页

当前保留模块：

- 账户
- 界面语言
- LLM 配置
- Webhook 推送
- API 用量

## 8.5 摘要详情页

摘要详情页以“本次任务”为基础，按“本次任务实际抓取到的关键词”拆成多张关键词卡片。

支持：

- 查看摘要
- 分享
- 复制 Markdown
- 下载 `.md / PDF / EPUB`
- 导出到 Obsidian / Notion
- 点赞 / 点踩
- 收藏
- 重新生成
- 删除

## 9. 业务规则

## 9.1 同一天复用规则

- 系统默认以“词条”为粒度控制抓取频率
- 同一词条一天最多抓取一次
- 如果当天已经抓取过，则新的抓取任务直接复用当天已有 `crawl_results`

这样做的目的：

- 降低重复抓取成本
- 降低外部源请求次数
- 降低摘要生成成本

## 9.2 清空历史优先级

如果用户显式清空历史，则系统认为用户要主动打破“当天复用”规则。

因此：

- 清空当前词条，只影响该词条
- 一键清空全部，影响当前用户全部词条

## 9.3 摘要生成规则

- 摘要只基于当前任务中“实际抓取到内容”的词条
- 不能混入用户后来新增的关键词
- 不能回退去展示“用户最近另一条摘要”
- 如果某个关键词没有有效内容，可以输出“今日新增有效信息有限”之类的轻量提示

## 9.4 中文输出规则

当用户界面语言为中文时：

- 摘要尽量输出为简体中文
- 英文、日文、韩文、繁体中文等内容应尽量转为简体中文表达
- 原始抓取数据和来源 URL 保持不变

## 9.5 来源链接规则

- 链接应跟在对应要点的末尾
- 可见文本应为短来源名
- 不应在页面中直接展示长裸 URL

## 10. 系统架构概览

系统当前采用前后端分离 + 异步任务队列架构。

### 10.1 组件划分

#### 前端

- Next.js 14 App Router
- 负责页面展示、任务轮询、关键词操作、历史查看、导出入口

#### 后端 API

- FastAPI
- 负责认证、业务接口、历史聚合、摘要详情、设置管理、导出和公开分享

#### 队列与异步任务

- Celery + Redis
- 负责执行抓取任务与摘要生成任务

#### 数据库

- PostgreSQL
- 保存用户、关键词、抓取任务、抓取结果、摘要、反馈、收藏、配置等

#### 抓取服务

- `requests + feedparser + BeautifulSoup + readability-lxml`
- 默认通过 Google News RSS 获取候选文章，再抽取正文

#### JS 渲染服务

- Playwright 微服务
- 用于处理需要浏览器渲染的页面

## 10.2 后端入口

后端入口文件是：

- [backend/app/main.py](/Users/mac/Documents/VSproject/Information-platform/backend/app/main.py)

它负责：

- FastAPI 应用初始化
- 中间件与限流配置
- 各路由注册
- 启动时表结构兜底补齐
- 搜索扩展（`pgvector`、`pg_trgm`、`pg_jieba`）初始化
- 初始化首个管理员账户
- `/health` 健康检查

## 10.3 Celery 入口

异步任务入口文件是：

- [backend/app/tasks/celery_app.py](/Users/mac/Documents/VSproject/Information-platform/backend/app/tasks/celery_app.py)

当前只注册两个核心任务模块：

- `crawl_tasks`
- `digest_tasks`

这反映了当前产品已经收敛到两条主异步链路：

1. 抓取
2. 摘要生成

## 11. 后端模块分层

从职责上看，后端可分为五层：

### 11.1 Router 层

负责 HTTP API 暴露。当前主要包括：

- `auth`
- `keywords`
- `crawl_jobs`
- `digests`
- `settings`
- `admin`
- `public`
- `stats`
- `export`

### 11.2 Model 层

当前仍在使用的核心模型包括：

- `User`
- `Keyword`
- `CrawlJob`
- `CrawlResult`
- `Digest`
- `DigestFeedback`
- `DigestStar`
- `UserLlmConfig`
- `UserNotificationConfig`
- `UserNotionConfig`
- `AuditLog`

### 11.3 Service 层

当前核心服务包括：

- 抓取服务
- LLM 服务
- 文本规范化服务
- 通知服务

这些服务承接“纯业务逻辑”和“外部系统调用”。

### 11.4 Task 层

由 Celery 执行异步任务，当前核心包括：

- `run_crawl_job`
- `generate_digest`

### 11.5 Database / Core 层

负责：

- 数据库连接
- 安全（密码哈希、API Key 加密等）
- 配置读取
- 限流

## 12. 异步任务链路

## 12.1 触发阶段

前端点击“立即抓取”后：

1. API 创建 `crawl_jobs`
2. Celery 异步消费 `run_crawl_job`
3. 前端轮询任务列表

## 12.2 抓取阶段

`run_crawl_job` 的主要流程是：

1. 读取当前用户
2. 创建或读取抓取任务
3. 标记任务为 `running`
4. 读取当前启用关键词
5. 对每个关键词判断当天是否已有结果
6. 如果已有，复用当天结果
7. 如果没有，则执行真实抓取
8. 为每个关键词写入 `crawl_results`
9. 更新 `summary_expected`
10. 若存在有效内容，则派发 `generate_digest`

### 当前实现特征

- 默认没有用户自定义 URL 时，系统会构造 Google News RSS 搜索 URL
- 抓取阶段并不是并发抓所有词，而是 worker 单并发串行执行
- 这意味着总耗时近似为“单关键词抓取耗时之和”

## 12.3 摘要阶段

`generate_digest` 的主要流程是：

1. 读取本次任务所有 `crawl_results`
2. 提取“本次任务实际使用到的关键词”
3. 读取用户 LLM 配置和界面语言
4. 组装按关键词分组的抓取内容
5. 调用 LLM 生成 Markdown 摘要
6. Upsert `digest`
7. 写回任务 `completed_at`
8. 如配置 Webhook，则发送通知

### 当前实现特征

- 摘要生成是单次 LLM 请求，不再额外同步做重要性评分
- `completed_at` 以摘要真正可查看为准
- 若 LLM 限流、超时或鉴权失败，会记录 `digest_error` 或走重试

## 13. 抓取与摘要性能特征

## 13.1 当前性能现实

当前抓取任务的耗时主要由两部分组成：

- 抓取阶段
- 摘要阶段

抓取阶段慢的原因通常是：

- 每个关键词要访问 RSS
- RSS 中还要跟进文章页做正文抽取
- worker 当前为单并发

摘要阶段慢的原因通常是：

- 关键词数量多
- 输入正文较长
- 模型响应慢
- 第三方 LLM 限流或超时

## 13.2 当前优化方向

项目已经做过的优化包括：

- RSS 跟进文章数设上限
- 内容够用后提前停止，不再抓满
- 收紧单关键词送入 LLM 的内容长度
- 收紧摘要输出 token 预算
- 前端在生成完成后立即停止计时

## 14. 数据模型关系

可以把当前核心数据关系理解为：

### 14.1 User

用户是一切数据的拥有者。

一个用户拥有：

- 多个关键词
- 多个抓取任务
- 多个摘要
- 一套 LLM 配置
- 一套通知配置
- 若干反馈与收藏记录

### 14.2 Keyword -> CrawlResult

关键词不会直接产生摘要。它先在某次抓取任务里形成抓取结果。

### 14.3 CrawlJob -> CrawlResult -> Digest

一条抓取任务包含多条抓取结果；其中有有效内容的结果会汇总成一条摘要。

因此本质关系是：

- `CrawlJob 1 -> N CrawlResult`
- `CrawlJob 1 -> 0/1 Digest`

### 14.4 Digest 的展示视图

虽然数据库中摘要是按任务存储，但前端还能从 digest 中聚合出“按词条历史查看”的视图，这是产品层的二次组织结果。

## 15. 前端实现结构

## 15.1 抓取任务页

主文件：

- [frontend/src/app/(dashboard)/dashboard/page.tsx](/Users/mac/Documents/VSproject/Information-platform/frontend/src/app/(dashboard)/dashboard/page.tsx)

当前页面承担的职责较多，集中处理：

- 抓取任务列表加载
- 关键词列表加载
- 历史词条加载
- 任务轮询
- 新增/删除关键词
- 智能推荐
- 分组创建/删除

这个页面是当前产品最重的前端页面之一。

## 15.2 词条设置页

主文件：

- [frontend/src/app/(dashboard)/digests/page.tsx](/Users/mac/Documents/VSproject/Information-platform/frontend/src/app/(dashboard)/digests/page.tsx)

页面主要负责：

- 加载词条历史概览
- 搜索词条
- 切换所选词条
- 加载该词条的历史摘要
- 清空当前词条历史
- 清空全部词条历史
- Markdown 渲染

## 15.3 任务状态轮询策略

抓取任务页会对“活动中的任务”执行轮询。

活动任务包括：

- `pending`
- `running`
- 处于“摘要生成中”的 `completed + summary_expected + !has_digest`

一旦拿到最终结束时间，前端就会冻结显示耗时，不再继续累加。

## 16. 搜索与扩展能力

启动时后端会尝试启用：

- `pgvector`
- `pg_trgm`
- `pg_jieba`

这说明当前系统虽然主流程是抓取与摘要，但也保留了为后续“摘要搜索 / 相似检索 / 中文全文搜索”继续扩展的基础。

当前这部分能力更偏底层准备，而不是主要产品卖点。

## 17. 通知与导出

## 17.1 通知

当前仅保留 Webhook 平台推送能力。

通知触发点主要在摘要生成成功后。

## 17.2 导出

当前摘要支持多种导出方式：

- Markdown
- PDF
- EPUB
- Obsidian
- Notion

这使得摘要既是“站内消费内容”，也可作为“站外流转内容”。

## 18. 已下线能力说明

为了避免误解项目边界，当前明确已下线且已从主流程移除的有：

- Email 通知
- 定时调度
- RSS / feed token
- 分组路由
- Web Push
- 旧来源管理

这些能力不应再作为现有产品的一部分进行介绍。

## 19. 当前主要风险点

### 19.1 抓取耗时风险

当关键词数量增多时，总抓取耗时线性上升，因为当前 worker 是单并发，且每个关键词都可能触发多次外部请求。

### 19.2 LLM 外部依赖风险

摘要性能和稳定性高度依赖外部 LLM 提供方，包括：

- 响应时延
- 限流
- 超时
- 鉴权错误

### 19.3 页面职责过重

当前抓取任务页承担了较多状态管理和用户操作，后续如果继续扩功能，可能需要再拆前端模块。

### 19.4 多语言与来源规范化复杂度

项目当前已经处理：

- 多语言摘要统一为简体中文
- 繁转简兜底
- 长 URL 收口为短来源名

这部分规则越多，后续越容易出现边缘格式问题。

## 20. 未来可扩展方向

如果继续演进，这个项目较自然的方向包括：

- 抓取并发优化
- 更细粒度的抓取耗时监控
- 摘要缓存与模型切换策略
- 摘要搜索与语义检索
- 更细的团队协作与共享空间
- 可配置抓取优先级与来源白名单

## 21. 一句话总结

当前的 Info Platform 本质上是一个“以关键词为中心、以任务为执行单元、以摘要为最终交付物”的多用户信息追踪系统；产品形态已经从早期的多杂项功能收敛为“抓取、摘要、历史、导出、推送”五条主能力线。
