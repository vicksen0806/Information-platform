# Info Platform PRD

更新日期：2026-04-12

## 1. 产品概述

Info Platform 是一个面向多用户的信息抓取与摘要平台。用户通过维护一组持续关注的关键词，手动触发抓取任务，系统从公开网页与 Google News RSS 聚合内容中提取信息，并基于 LLM 生成按关键词拆分的摘要卡片，最终在站内查看、分享、导出，或通过 Webhook 推送给外部平台。

产品定位不是“全网搜索引擎”，而是“轻量级、可持续追踪的个人/团队信息看板”。

## 2. 产品目标

- 帮助用户围绕固定主题持续追踪信息，而不是每次重复检索。
- 将原始抓取内容压缩为可快速阅读的关键词摘要，降低信息处理成本。
- 提供明确的任务状态与耗时反馈，让用户知道一次抓取实际花了多少时间。
- 保留词条级历史记录，支持用户按主题回看并在需要时重新抓取。

## 3. 目标用户

- 需要持续跟踪行业、公司、技术或地区话题的个人用户
- 需要定期收集公开舆情、行业动态、竞品动态的小团队
- 需要把外部信息沉淀为可分享、可导出的简报内容的知识工作者

## 4. 核心使用流程

1. 用户在“抓取任务”页新增关键词。
2. 用户可使用“智能推荐”补充更多关键词。
3. 用户点击“立即抓取”，系统按当前关键词集合创建抓取任务。
4. 后端按关键词逐个抓取内容，并生成本次任务的词条结果。
5. 若抓取到有效内容，系统调用 LLM 生成摘要。
6. 前端实时展示任务状态，并在摘要可查看后立即刷新为完成态。
7. 用户进入摘要详情页查看、分享、导出或收藏。
8. 用户可在“词条设置”页按词条查看历史，并清空当前词条或全部词条历史，以便同一天重新抓取。

## 5. 功能范围

### 5.1 抓取任务

- 展示当前关键词列表与分组
- 支持手动新增、删除关键词
- 支持基于当前关键词生成分组
- 支持手动触发抓取
- 展示任务状态：抓取中、生成摘要中、已完成、失败
- 展示任务耗时，统计口径为：任务下发到摘要生成完成

### 5.2 智能推荐

- 基于用户近 15 天使用/抓取过的关键词生成推荐词
- 推荐结果应为短关键词，而非句子或新闻标题
- 用户可一键加入推荐词

### 5.3 词条设置

- 按词条维度查看历史，而不是按批次任务维度查看
- 展示历史抓取时间与摘要内容
- 支持搜索历史词条
- 支持“清空当前词条”
- 支持“清空所有词条”
- 清空后允许同一词条在同一天重新抓取

### 5.4 摘要详情

- 按关键词展示独立摘要卡片
- 中文界面下，输出尽量统一为简体中文
- 来源链接以短来源名展示，不直接暴露长 URL
- 支持复制 Markdown、分享、下载 `.md / PDF / EPUB`
- 支持点赞、点踩、收藏

### 5.5 系统设置

- 账户信息管理
- 界面语言切换
- LLM 配置
- Webhook 推送配置
- API 用量查看

## 6. 关键规则

- 抓取以“词条”为最小单位执行。
- 同一词条默认一天最多抓取一次；若当天已有结果，则复用当天内容。
- 清空历史后，该词条不再复用旧结果，可在同一天重新抓取。
- 删除词条配置时，不删除历史记录。
- 摘要生成仅基于“本次任务实际抓取到的词条”，不能混入其他词条内容。
- 前端在摘要生成完成后应立即刷新状态，不允许已完成任务继续累积耗时。

## 7. 非功能要求

- 本地开发默认采用 Mac 原生启动方式。
- 抓取任务需向用户明确展示实时状态与可感知耗时。
- 抓取服务应控制单次任务耗时，避免单个关键词跟进过多文章。
- LLM 调用失败、限流或超时需要有明确错误提示或重试机制。
- 原始抓取数据与来源 URL 不应被展示层改写。

## 8. 当前技术方案

- 前端：Next.js 14 App Router + Tailwind CSS
- 后端：FastAPI
- 数据库：PostgreSQL
- 队列：Celery + Redis
- 抓取：requests + BeautifulSoup + readability-lxml + feedparser
- 摘要：OpenAI 兼容接口接入第三方 LLM
- 渲染：Playwright 微服务处理需要 JS 的页面

## 9. 成功标准

- 用户能够在 1 次操作内完成“新增关键词 -> 抓取 -> 查看摘要”
- 任务状态与最终耗时对用户可解释、可追踪
- 摘要内容按关键词清晰拆分，无跨词条混杂
- 历史记录支持用户回看、清空和重新抓取
- 导出与分享满足基础对外展示需求

## 10. 当前不在范围内

- 邮件通知
- 定时调度抓取
- RSS / Feed Token 对外订阅
- Web Push
- 旧来源管理后台
- 复杂权限体系以外的企业级协作能力
